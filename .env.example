# shellcheck disable=SC2034
UID=501
GID=20

# API Secret Key for internal communication between Next.js and Go backend
# Generate a strong random key (hex encoding is safe for environment variables):
# openssl rand -hex 32
API_SECRET_KEY=your-secret-key-here

# Database credentials
DB_USER=user
DB_PASSWORD=password

# GitHub API Token (required for repository data collection)
GITHUB_TOKEN=your-github-token-here
